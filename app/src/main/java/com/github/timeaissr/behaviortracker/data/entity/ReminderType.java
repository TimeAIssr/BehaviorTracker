package com.github.timeaissr.behaviortracker.data.entity;

/**
 * Enum representing the type of reminder.
 * FIXED_TIME: Remind at a specific time each day.
 * INTERVAL: Remind at regular intervals within a time window.
 */
public enum ReminderType {
    FIXED_TIME,
    INTERVAL
}
